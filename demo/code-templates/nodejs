const GeowikiAPI = require('overpass-frontend')
const geowikiAPI = new GeowikiAPI(__URL__)

const request = geowikiAPI.BBoxQuery(
  __BBOXQUERY_PARAMS__
  (err, feature) => console.log(feature.id, feature.tags),
  (err) => console.error(err)
)
